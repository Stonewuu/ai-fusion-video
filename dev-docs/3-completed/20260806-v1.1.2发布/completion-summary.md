# v1.1.2 发布完成总结

## 最终范围

- Maven 项目版本、SCM tag 和前端包版本更新为 `1.1.2`。
- Pipeline 历史重建支持结果投影缺少调用投影、结果先到及父工具后到。
- 字符串化 JSON 改为按字段脱敏；普通文本和历史脱敏占位符可安全查看 JSON。
- 根应用、Dashboard 和关键功能区域增加错误隔离、恢复面板及顶部错误提示。
- 全局错误监听与 API Toast 统一过滤请求取消、Next.js 内部控制流和自动 Token 续期错误。

## 验证结果

- `pnpm exec tsc --noEmit`：通过。
- 变更文件 ESLint：通过。
- 前端 Node 测试：18/18 通过。
- 后端相关测试：21/21 通过。
- 真实历史 Pipeline 中 `[FILE_PATH_REDACTED]` 可切换为合法 JSON 字符串视图，无白屏或新增控制台错误。
- `git diff --check`：通过。

## 发布结果

- `main` 分支包含全部修复与版本更新。
- GitHub 正式 Release：`v1.1.2`，非草稿、非预发布。

## 遗留事项

- 既有脱敏占位符无法恢复原始结果，其余无新增遗留事项。
