# 技术债务

## 已有历史数据限制

已经持久化为 `[FILE_PATH_REDACTED]` 的工具结果，其原始内容在事件清洗阶段已经被替换，无法仅依赖 `afv_agent_message` 或已清洗事件恢复。前端现可安全展示和查看这些占位符；后端修复只保证后续字符串化 JSON 按字段清洗并保留其余结构。

除上述不可逆历史数据外，本次未新增技术债务。历史时间线将“结果先到”和“仅有结果投影”作为可持久化状态直接建模，不依赖吞异常。
